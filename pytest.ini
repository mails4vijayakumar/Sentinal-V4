[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short -q

markers =
    unit: pure unit tests (no I/O)
    integration: requires running compose stack
