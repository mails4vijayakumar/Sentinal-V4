# ── Shared agent dependencies ─────────────────────────────────────────────────
# Each agent container installs this file plus any agent-specific additions.

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# HTTP client
httpx==0.27.2
truststore==0.9.2  # OS trust store integration for corporate-CA TLS interception

# Data validation
pydantic==2.8.2
pydantic-settings==2.4.0

# Redis
redis[asyncio]==5.0.8

# Database (asyncpg for direct connections; psycopg2-binary for pgvector)
asyncpg==0.29.0

# Auth / crypto
python-jose[cryptography]==3.3.0

# Logging
python-json-logger==2.0.7
